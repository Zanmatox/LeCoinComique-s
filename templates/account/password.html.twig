{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Le Coin Comique's{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passse</h1>
    - <a href="{{ path('account') }}">Retourner à mon espace compte</a><br/>
    <hr>
    {% if notification %}
        <div class="aleret alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}
    <hr>
{% endblock %}