{% extends 'base.html.twig' %}

{% block title %}Valider ma commande - Le Coin Comique's{% endblock %}

{% block content %}
    <h2> Je passe ma commande</h2>
    <p> Choississez vos préférences avant de passer voter commande sur Le Coin Comique's.</p>
    <hr>
    <div class="row">
        <div class="col-md-6">
            {% set formHtml %} 
                {{ form_start(form, {action:path('commande_recap')}) }}
                {{ form_end(form) }}
            {% endset %}

            {{ formHtml|replace({'[br]' : '<br/>'})|raw }}

        </div>
        <div class="col-md-6"> 
    <div class="text-center">
    <b>Récap de ma commande</b><br/>
    <p>Retrouver le récapitulatif de vos produits.</p><br/>
    </div>
    <div class="commande-summary">
    {% for key,product in cart %}
        <div class="row {% if key > 0 %}mt-2{% endif %}">
            <div class="col-2"> 
                <img src="/uploads/{{ product.product.illustration }} " alt="{{ product.product.titre }}" height="75px">
            </div>
            <div class="col-8 my-auto">
                    {{ product.product.titre }}
                    <small>{{ product.product.auteur }}</small>
            </div>
            <div class="col-2 my-auto">
                    x {{ product.quantity }}
                </div> 
            </div> 
    {% endfor %}
    </div>
    </div>
{% endblock %}
